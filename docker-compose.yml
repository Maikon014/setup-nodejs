version: '3'

services:
  app:
    container_name: setup-nodejs
    build: 
      context: . 
      dockerfile: Dockerfile
    volumes:
      - ./:/usr/src/app
    entrypoint: 
      - PORT=3000
    ports:
      - 3000:3000 